overrides:
  nanoid: 3.3.18
  postprocessing: ^6.39.4
  undici: ^8.10.0
  vite: ^8.2.1

# Explicitly reviewed latest release; required by this repository refresh while
# the global minimum-release-age policy is still cooling it down.
minimumReleaseAgeExclude:
  - "@types/node"
